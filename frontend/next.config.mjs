/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'via.placeholder.com',
            'www.multifoodwindsor.com',
            'assets.shop.loblaws.ca',
        ],
    },
}

export default nextConfig
